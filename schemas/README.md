Runtime schema and templates live in the installable package:

`src/mouse_dpi_tool/resources/schemas/mouse_dpi_tool_session_v1.json`

Load via `mouse_dpi_tool.resources.session_schema()`.
Do not keep a second copy at the repository root.
